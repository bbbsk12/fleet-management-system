# 调度内核规则 v2

## 0. 目标与适用范围
- 目标：在保证安全不碰撞的前提下，提升通过效率，避免死锁、饥饿和长时间假阻塞。
- 范围：适用于所有参与地图交通拓扑的底盘（含任务车、空闲车、避让车）。

## 1. 术语定义
- 航点（Waypoint）：交通图中的离散点位。
- 航线（Segment/Edge）：连接两个航点的无向边。
- 占用锁（Lock）：机器人当前物理位置对应的资源锁。
- 预约锁（Reservation）：机器人下一跳/目标点的短期前瞻锁。
- 可让行机器人：允许被系统调度去避让的机器人。

## 2. 核心安全规则（保留并细化）
1. 同点同线唯一占用  
   同一时刻，同一航点、同一无向航线（不区分方向）仅允许一台机器人占用或预约。

2. 可让行资格规则（替代原规则②）  
   仅可让行机器人可被调度执行避让。以下状态禁止被调度让行：  
   - 离线/不可通信；  
   - 正在执行底盘动作（executing）；  
   - 处于硬保持（hard hold）且绑定用户任务恢复上下文；  
   - 正在关键导航动作中且不可安全抢占。

3. 航点邻接排他  
   当机器人占用航点 W 时，W 的所有邻接航线进入排他区。  
   仅允许满足安全放行条件的同机器人续行，不允许其他机器人汇入冲突区域。

4. 航线端点排他  
   当机器人占用航线 E(A-B) 时，A/B 两端点进入受限区。  
   未获得合法预约的其他机器人不得进入该航线及其冲突端点。

## 3. 优先级与冲突裁决规则
5. 冲突判定优先级  
   按顺序判定：同点同线冲突 > 邻接排他冲突 > 汇入冲突 > 对向冲突。

6. 首跳原子检查规则  
   首跳必须执行“可进入检查 + 资源预约”原子化流程，禁止先发车后补锁。

7. 汇入公平规则（防饥饿）  
   多车汇入同一目标航点时，按等待时长提升优先级（aging）；等待超时可强制晋升。

## 4. 生命周期与恢复规则
8. 预约TTL规则  
   所有预约锁必须设置超时时间（TTL），超时自动释放，防止异常残留锁永久封路。

9. 离线幽灵锁规则  
   机器人离线时保留其位置锁（防撞），释放其预约锁；恢复在线后重建实时锁。

10. 死锁检测与破局规则  
   持续检测阻塞环；达到阈值后选择代价最小的“让行牺牲者”执行避让，打破环路。

11. 任务状态一致性规则  
   任务状态与导航状态不一致时，系统应自动修复：回退 pending 或标记 failed，并释放无效锁。

12. 不可达与长阻塞失败规则  
   被离线/不可移动资源持续阻塞超过阈值时，应将受影响任务失败化并给出明确原因码。

## 5. 实施要求
13. 单一真值源  
   所有“能否进入/能否预约”的判断统一由占用管理器执行，禁止业务侧私自绕过。

14. 规则可观测性  
   每次拒绝通行、锁失败、死锁打破、超时释放均必须记录结构化日志（含 robot/task/reason）。

15. 规则变更兼容性  
   新增规则需保持向后兼容，至少支持“安全优先模式”和“效率优先模式”两个策略档位。




